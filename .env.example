# URL of the sitewyre-backend API (e.g. its Vercel deployment URL, no
# trailing slash). Server-only — never exposed to the browser. Leave blank
# to use the static fallback data in lib/projects.ts, lib/services.ts, and
# lib/testimonials.ts, and to keep the contact form's log-only stub —
# this is the current production default.
BACKEND_URL=
